# B2.1 Failure Triage

**Review status:** Completed as AI independent technical review. This document does not represent human signoff and does not open any qualification gate.

## Triage matrix

| Failure | Root cause | Classification | Safe action taken | Final status |
|---|---|---|---|---|
| `test_option_b_import_validates_and_original_packet_stays_pending` | Historical Option B import pins the original approval attachment to SHA-256 `2784b4746e96419a3dadea2e765d58a9dbc719b283bb683b38bbe95c5850230b`, while the mutable current upload at the same path has a different digest. | Historical evidence/provenance drift | Preserved the pinned import and pending packet; did not replace the attachment or alter the hash. | Expected fail-closed failure; unresolved without the original attachment or an explicit provenance decision. |
| `test_option_b_runner_emits_only_redacted_blocked_records` | Same approval-source hash mismatch occurs before the legacy runner can build its result. | Downstream symptom of historical evidence drift | No runner behavior was weakened. B2.1 uses a separate campaign and hash-only provenance. | Expected fail-closed failure; unresolved. |
| `test_option_b_runner_records_runtime_digest_blockers_without_sensitive_material` | Same approval-source hash mismatch in the legacy runner boundary. | Downstream symptom of historical evidence drift | Kept old boundary intact and retained redaction/invariant checks. | Expected fail-closed failure; unresolved. |
| `test_option_b_runner_exposes_offline_harness_readiness_separately` | Same approval-source hash mismatch in the legacy runner boundary. | Downstream symptom of historical evidence drift | Kept offline/target-live separation unchanged. | Expected fail-closed failure; unresolved. |
| `test_inventory_validator_passes` | Pinned `/tmp/juice-shop-source` tree was absent in the fresh working environment, so source evidence could not be re-hashed. | Reproducibility/environment fixture gap | Re-cloned Juice Shop and checked out revision `1618a611b173b4bf114028e6e02549950606e29d`; the validator then passed. | Fixed; validator PASS. |

## Verification

After restoring the pinned Juice Shop source tree, `check_source_backed_candidate_inventory.py` returned:

```text
PASS: source-backed candidate inventory is valid and fail-closed
targets=3 accepted=3 blocked=11 observation_only=3 out_of_scope=11
```

The four remaining legacy failures are intentionally preserved as fail-closed signals. The source-backed inventory failure is fixed after restoring the pinned Juice Shop tree. Making the four approval-import failures pass would require either recovering the exact historical approval attachment or changing the historical import contract. Neither action is safe to perform implicitly, because it would alter or obscure provenance for a prior approval boundary. A later full-suite attempt reproduced those four failures and reached 98% before stopping without a final pytest summary; the targeted B2.1 and inventory gates remained green.

## Recommendation

Do not modify `LOCAL-CAUSAL-LAB-OPTION-B-OWNER-APPROVAL-IMPORT-v1.json`, do not replace the historical attachment with `pasted_content_2.txt`, and do not bypass the validator. If the historical tests must be restored to green, obtain the exact original attachment or create a separately approved provenance-migration decision packet. Until then, report the four failures as intentional historical-drift blockers rather than B2.1 regressions.

**Author:** Manus AI — non-human attributable technical review.
