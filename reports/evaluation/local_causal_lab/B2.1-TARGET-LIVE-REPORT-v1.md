# B2.1 Target-Live Report — WebGoat IDOR

**Status:** `INCONCLUSIVE` for WebGoat IDOR; `BLOCKED` for crAPI object access.

**Scope:** B2.1 approved local causal-lab directive, WebGoat IDOR only, loopback origin, normal local lesson authentication, GET-only observations, synthetic opaque identity labels, no raw response/body/header/cookie/token/credential persistence, no external destination, no Official P10, and no Bug Bounty activity.

## Execution result

The WebGoat runtime was freshly attested against source revision `7517acca95d9851da706452454c223dd13545ef4`, the pinned jar digest, the pinned Java binary digest, a loopback-only listener, and an open descriptor for the built jar. The precondition gate was therefore ready for the bounded local cycle.

The runner executed a baseline, a candidate request for another synthetic profile, and an independent nonexistent-profile control. The three redacted observations returned the same redirect-level semantic shape (`302`) and did not expose a completed lesson or a distinguishing IDOR signal. The causal predicate was therefore not satisfied. This is **not a confirmed vulnerability and not a detection failure**; it is an `INCONCLUSIVE` target observation.

Because the oracle was inconclusive, the runner withheld the scoring ProofBundle. No seal, seal verification, replay result, or metrics were created for this case. This is required by the fail-closed scoring contract.

crAPI remained `BLOCKED` because requester/owner fixture injection and reset could not be proven without additional target state mutation or credential/token material. No crAPI request was made.

| Case | Classification | Network requests | Causal signal | ProofBundle | Reason |
|---|---|---:|---:|---|---|
| `webgoat.idor.view_other_profile.v1` | `INCONCLUSIVE` | 3 GET | No | Withheld | Candidate and controls had indistinguishable redirect semantics; LessonSession was not causally available. |
| `crapi.object_access.b2` | `BLOCKED` | 0 | Not evaluated | Withheld | Safe requester/owner fixture injection and reset were unavailable under the approved boundary. |

## Governance state

`official_isolated_p10_runs_authorized=false`, `P10=NOT_QUALIFIED`, `P9=NOT_QUALIFIED`, `VIP=NOT_QUALIFIED`, and `Bug Bounty=BLOCKED` remain unchanged. B2.1 is local validation evidence only and is not an Official P10 run.

## Evidence hygiene

Only bounded semantic fields and response digests were written. Raw response bodies, headers, cookies, tokens, credentials, process arguments, and environment values were not persisted. The B2.1 authorization record references `pasted_content_2.txt` by filename and SHA-256 only; the raw approval text is not stored in the repository.

## Remaining blockers

The WebGoat case needs a target-local, disposable lesson-session fixture or another approved safe canary that makes the causal IDOR predicate observable without relying on ambiguous redirect behavior. crAPI needs a disposable owner/requester fixture injection path and reset verification that fit the approved boundary. Until those conditions change, no scoring promotion is justified.

## Validation note

The B2.1 runner regression checks passed. The full-suite triage initially identified five failures: four downstream failures caused by the historical Option B approval import validating the mutable external attachment path against its pinned historical SHA-256, plus one source-backed inventory fixture failure. The inventory failure was fixed safely by restoring the pinned Juice Shop source tree; its validator now passes. A subsequent full-suite attempt reproduced the four historical approval-import failures and progressed to 98% before the test process stopped without emitting a final pytest summary; no new B2.1 failure was observed. The historical approval record was not rewritten, and the current attachment was not substituted into that historical record, because doing so would alter evidence provenance and prior scope.

**Author:** Manus AI — non-human attributable technical review.
